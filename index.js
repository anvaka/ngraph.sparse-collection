/**
 * This library is huge. Import concrete graph you are interested in, like so:
 * ``` js
 *   var mtxObject = require('./HB/plat1919'),
 *       mtxParser = require('ngraph.serialization/mtx'),
 *       graaph mtxParser.loadFromObject(mtxObject);
 * ```
 */

